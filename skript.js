const express = require('express')
const app = express()

app.use(() => {
  Haså
}

        
        fler vingar

        app.use('request', (res, req) => {
          console.write('Skriv dentta om du kan')
        })
